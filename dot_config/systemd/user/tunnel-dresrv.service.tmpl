[Unit]
Description=Tunnel Ports to DreSRV
After=caddy.service
Requires=caddy.service

[Service]
Restart=always
RestartSec=5
ExecStartPre=bash -c "while ! ping -q -c 1 -W 1 {{ .dresrv.domain }} > /dev/null; do sleep 1; done"
ExecStart=ssh -N prom-tunnel

[Install]
WantedBy=default.target
