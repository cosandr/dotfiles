#!/bin/sh

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
    exec /usr/bin/slack --silent --ozone-platform=wayland --enable-features=WaylandWindowDecorations "$@"
fi

exec /usr/bin/slack --silent "$@"
