#!/bin/sh

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
    exec /usr/bin/discord --enable-features=UseOzonePlatform,WebRTCPipeWireCapturer --ozone-platform=wayland "$@"
fi

exec /usr/bin/discord "$@"
