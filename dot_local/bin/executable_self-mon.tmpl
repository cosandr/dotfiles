#!/usr/bin/env bash

{{- if lookPath "nvtop" }}
kitty -c kitty.conf -c pure.conf --detach --title self-monitoring tmuxinator start hjn
{{- else }}
kitty -c kitty.conf -c pure.conf --detach --title self-monitoring tmuxinator start hj
{{- end }}
